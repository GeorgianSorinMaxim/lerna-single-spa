{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run watch:portal --stream",
    "update-version": "lerna publish --skip-git --skip-npm --force-publish '*'"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
